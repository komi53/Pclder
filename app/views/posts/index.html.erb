<h1>Posts#index</h1>
<p>Find me in app/views/posts/index.html.erb</p>
<% if @posts %>
	<% @posts.each do |post| %>
	 <tr>
	    <td><%= link_to  post_path(post.user.id) do %>
            <%= attachment_image_tag post, :post_image, :fill, 40, 40, format: 'jpeg', fallback: "no_image.jpg",:size =>'40x40' %>
          <% end %>
      </td>
      <td><%= link_to post.title, post_path(post.id) %></td>
      <td><%= post.title %></td>
           <% post.tag_list.each do |tag| %>
            <%= link_to tag, posts_path(tag_name: tag), class: "badge badge-success p-1 badge-tag",data: {"turbolinks" => false} %>
          <% end %>
   </tr>
   By<%= post.user.name %>
   <%= link_to "#{post.post_comments.count} コメント", post_path(post.id) %>
   <% end %>
    aaaaaaaaaaaaaaaa

<% elsif @q_posts %>
  <% if @q_posts.present? %>
    <% @q_posts.each do |post| %>
      <tr>
        <td><%= link_to  post_path(post.user.id) do %>
              <%= attachment_image_tag post, :post_image, :fill, 40, 40, format: 'jpeg', fallback: "no_image.jpg",:size =>'40x40' %>
            <% end %>
        </td>
        <td><%= link_to post.title, post_path(post.id) %></td>
        <td><%= post.title %></td>
        <% post.tag_list.each do |tag| %>
          <%= link_to tag, posts_path(tag_name: tag), class: "badge badge-success p-1 badge-tag",data: {"turbolinks" => false} %>
        <% end %>
      </tr>
    By<%= post.user.name %>
    <%= link_to "#{post.post_comments.count} コメント", post_path(post.id) %>
    <% end %>
    <%= form_tag(search_users_path, method: :get) do %>
     <%= hidden_field_tag 'search[content]', @q.title_cont %>
        <button type="submit" class="btn btn-success">
            ユーザー
          </button>
    <% end %>
  <% else %>
    ヒットしませんでした。
    <%= form_tag(search_users_path, method: :get) do %>
     <%= hidden_field_tag 'search[content]', @q.title_cont %>
        <button type="submit" class="btn btn-success">
            ユーザー
          </button>
        <% end %>
  <% end %>
<% end %>